server:
  port: 5555

spring:
  application:
    name: zuul-service

  profiles:
    active:
      dev

  cloud:
    config:
      allowOverride=true
      enabled: true
      uri: http://localhost:8888
