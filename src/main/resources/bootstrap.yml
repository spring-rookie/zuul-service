server:
  port: 5555

spring:
  application:
    name: zuul-service

  profiles:
    active:
      dev

  cloud:
    config:
      overrideNone: true
      enabled: true
      uri: http://localhost:8888
